# Upstream and strip log

This folder is a stripped fork of **God's Eye View** by Bilawal Sidhu, <https://github.com/bilawalsidhu/gods-eye-view>, imported at commit **`f01b6a5`** ("fix(ui): stop panel glows from adding scrollbars to the narrow-screen rails (#699)"). Its MIT licence is in `LICENSE`. The MIT grant covers the source code only; upstream states that its third-party data and assets are excluded. See `THIRD_PARTY_NOTICES.md` at the repository root.

Plan: `docs/superpowers/plans/2026-09-24-m5-visual-shell.md`. Branch: `m5-shell`. Each step below is one commit.

## What was left out of the import (deviations from "unmodified")

- `.git`, `node_modules`: not source. (`build/` is source here, not output: `build/application-html.js` is imported by tests, and my first import wrongly excluded it. Restored in A1b.)
- `docs/media/` (about 69 MB of demonstration GIFs) and `.github/` are not imported. Upstream's `docs/*.md` (512 KB) are imported, because 23 test files assert documentation consistency.
- `src/data/local_data/`, `src/data/fixtures/`, `public/models/`, `public/events/` are **on disk but ignored by git** (`.gitignore` here). They are upstream's bundled datasets, test fixtures, 3D models and event media, whose licences are not cleared for this repository. (`src/data/` itself also holds 236 code files. A1 ignored the whole directory by mistake, so those files were absent from history until the correction recorded as row A1c.) The app still needs them to start until the strip steps that own them remove the code that reads them; each such step deletes its files. They never enter git history.

## Step log

| Step | What changed | Tests | Smoke | Notes |
|---|---|---|---|---|
| A1 | Imported the reference code unmodified (apart from the exclusions above) | not yet run | not yet run | 961 tracked files |
| A1b | Restored `build/` and imported `docs/*.md` after a first run showed 23 failing test files caused by my exclusions | 4168 tests, 4158 pass, 0 fail, 10 skipped: identical to the untouched clone | not yet run | the mistake and its fix are recorded rather than hidden |
| A2 | `npm ci` (PUPPETEER_SKIP_DOWNLOAD=1) and first run | as A1b | renders | 15 s install; upstream's lockfile already resolves **one** `@cesium/engine` (22.3.0, `@cesium/widgets` 14.3.0 deduped) |
| A3 | Baseline measurements | as A1b | | `vite build`: 423 files, 31.2 MB in `dist/` (Cesium.js 5.6 MB, egm96 2.7 MB, datacenters.geojsonl 2.5 MB, regions 1.9 MB); `check:boundaries` passes |
| A4 | Added the step checks: Playwright smoke (`tests/smoke.spec.mjs`, `known-errors.json`), `scripts/nexus-dead-modules.mjs` (baseline: 1147 modules, 328 tests, 681 reachable; 2 dangling and 40 unreachable recorded as the allowed list), `scripts/nexus-check-engine.mjs`, and `npm run check:step` | as A1b | 0 console errors; screenshot 755 KB (`.local/m5/a4-baseline.png`) | external hosts at load: `fonts.googleapis.com`, `fonts.gstatic.com`, `services.arcgisonline.com`, `terrain.reearth.land` |
| A5 | Removed the Google Fonts and Material Symbols CDN links. Fonts and a 32-glyph icon subset are self-hosted (`public/fonts`, 189 KB, hashes in `manifest.json`); `src/ui/styles/fonts.css` holds the `@font-face` rules and `icon-glyphs.css` (imported last, as Google's stylesheet was) holds the icon class. `scripts/nexus-fetch-fonts.mjs` reproduces it. The smoke test now asserts the page contacts only `tests/allowed-hosts.json` | 4168 tests, 0 fail | renders, 0 errors; hosts now `127.0.0.1`, `services.arcgisonline.com`, `terrain.reearth.land` | the first attempt put the icon class before the app CSS, which shrank the arrows; found by comparing screenshots, fixed by keeping Google's original cascade order |
| A1c | Found while removing voice code that `.gitignore` excluded all of `src/data/`, which holds 236 source and test files beside the datasets. Narrowed the ignore to `src/data/local_data/` and `src/data/fixtures/` and tracked the code | not re-run | | recorded out of order: this commit lands after B1, so B1's edits to `src/data` tests are inside it and `src/data` has no faithful A1 snapshot in history; the untouched reference is `f01b6a5` |
| B1 | Removed the upstream Node server (`server/`), the Pinokio launcher, dev/QA shell and puppeteer scripts, and the tests whose subject they were; `vite.config.js` is now the browser-only `createBrowserViteConfig` on 127.0.0.1:4173. Pruned 15 dangling `exports` (`scripts/nexus-prune-exports.mjs` reports and fixes them; `check:exports` is now part of `check:step`), 15 boundary groups, the `puppeteer` devDependency and the `track-regression` rig. `ws` and `sharp` stay until the AIS and transit-icon code they serve is removed | 3600 tests, 3593 pass, 0 fail, 7 skipped (568 fewer than A1b: tests deleted with the code they covered) | renders; 2 transitional 404s from `/api/realtime/debug-log` and `/api/openai/hud-summary` recorded in `known-errors.json`, to disappear with the voice code | dead-module baseline re-recorded (58 unreachable, 1 dangling); the smoke test now waits for a rendered frame instead of a fixed delay after one run screenshotted before imagery loaded |
| B2 | Removed voice: `src/voice/` (29 files, including the 2,400-line `gevActions.js`), the microphone dock control's owner, the HUD's OpenAI summary request (the HUD keeps its deterministic summary line), the `summary` request service and its endpoint, `hudSummaryResponse.js`, and `data/analystEngine.js` (only the voice model called it). Tests that read the deleted voice sources were removed or trimmed (three whole tests). New `scripts/nexus-prune-config.mjs` prunes deleted modules from `package.json` exports, `package-boundaries.json` and `format-scope.json` (`check:config` replaces `check:exports`); `format:check` joins `check:step` | 3342 tests, 3335 pass, 0 fail, 7 skipped | renders, 0 errors; both transitional 404s are gone and `known-errors.json` is empty again | screenshot compared with `.local/m5/a4-baseline.png`: same chrome; the mic pill is gone and the dock is shorter. Left for later steps: voice CSS (B15), the first-run tip "GEV MIC button" (B14), `check-import-directions.mjs` voice rules (B16) |
| B3.1 | Removed the submarine-cable layer (TeleGeography data, licence CC BY-NC-SA, uncleared): `layers/submarineCables`, its app constructor and bundled source, the layer-state token, catalogue entry, panel entry, credit line and the on-disk dataset. Also deleted the world-overlay allocation probe (`worldOverlayAllocation.{test,worker}.mjs`, 1,700 lines) because it benchmarks every live layer and would need edits at each step; the overlay itself stays | 3314 tests, 3307 pass, 0 fail, 7 skipped | renders, 0 errors | layer count assertions 21 to 20; `ALLOCATION_TEST_FILES` now holds one file |
| B3.2 (from B14) | Removed the first-run mission launcher early (`firstRunExperience.js`, its test, `welcome.html`, `first-run.css`). Its tiles (contacts, space missions, environmental) each turned on layers the next steps delete, so keeping it would have forced an edit at every step. `startApplicationChrome` keeps the reveal sequence and now takes the welcome initializer only by injection, which is where Nexus's own first-run will plug in (C) | 3282 tests, 3275 pass, 0 fail, 7 skipped | renders, 0 errors; the modal is gone (screenshot 1.28 MB) | B14 now only has the key-setup wizard left |
| B3.3 | Removed the earthquakes layer (USGS): `layers/earthquakes`, its app constructor and compat module, the reference-source factory that only served it and cables, the layer-state token, panel entry, credit and recipe keys. Registry tests that used `earthquakes` as a stand-in id now use `local-dams` (token `q`), which survives until B13 | 3267 tests, 3260 pass, 0 fail, 7 skipped | renders, 0 errors | layer count 19; `app/layers/overlayHost.js` deleted (only the quake constructor used it) |
| B3.4 | Removed the ground-mobility layers: bikeshare (GBFS), street traffic (TomTom flow, Overpass roads) and transit (GTFS-Realtime), with their compat data modules, camera-sensitivity claims, history store, feed credits and layer-state tokens. Removed the traffic sync chip (`shellFeedback`, `loadingFeedback` reducer, markup element, element lookups) and the traffic hooks in world-jump navigation; the loading ticker survives as `_startFeedbackTicker` and the timer is now `_worldJumpTimer`. New tool for test edits: a parser-based test remover (scratch), because a brace matcher had mangled a file earlier | 2945 tests, 2938 pass, 0 fail, 7 skipped (322 fewer: the transit lifecycle suite alone was ~3,700 lines) | renders, 0 errors | layer count 16. Detection still carries transit bracket painting and `worldOverlayTokens` transit tier colours until B12; CSS for the chip until B15; layer-registry tests now use `local-datacenters` as the stand-in id |
| B3.5 | Removed the radio layer (Radio Browser directory, audio playback) and everything built around it: the panel and its tuner, the compact dock in the Context header, the Cockpit radio launcher, `ui/radio*` modules, panel-chrome disclosure code, the `radio-panel` share token, layer-state option codec (filter and volume) and credits. Tests that read the deleted sources or exercised radio options were removed (about a dozen); generic layer-state tests now use `local-datacenters` as the slow-layer stand-in. New scratch tools: `ast_strip.mjs` (parser-based removal of properties, imports, declarations and calls) and `rm_tests.mjs --using` | 2859 tests, 2852 pass, 0 fail, 7 skipped | renders, 0 errors | layer count 15. `radio.css` waits for B15; `_setCockpitDisclosure` on the facade is now a no-op until the Cockpit goes |
| B3.6 | Removed the camera layers (CCTV and mapped ALPR cameras): both layers and their compat modules, the calibration/gizmo/viewshed/footprint/LOD/card data modules, focus-request and focus-policy plumbing, the CCTV panel (markup, controls, bindings, frame viewer), the sync chip, the `C` hotkey, the `cctv-panel` share token, the layer-state option codec, ALPR retry copy in the loading feedback, 13 credit entries and the 2 MB ground-height dataset. Scene tests now use a permanent fake id (`test-layer`) instead of real layer ids, and the recipe keys for removed layers were dropped | 2649 tests, 2646 pass, 0 fail, 3 skipped | renders, 0 errors | layer count 13 (the registry now lists: ais-live-vessels, bhote-koshi x2, directions, flights, local-dams, local-datacenters, local-firms, military, military-awareness, military-installations, rocket-launches, satellites). World-overlay tests that used the CCTV thumbnail entry were removed (8) |
| B3.7 (with B12) | Removed the whole live-contacts domain in one step, because its parts only stand together: the flight, military, awareness, installation, vessel, satellite, launch and FIRMS layers with their sources and data modules; the Context panel (Contacts, Space Missions, Radio host); the aircraft Cockpit view and its templates; the detection overlay (engine, drawing, presentation, share parameters, DISPLAY controls, `D` hotkey); the tracked readout, tr3b, the 3D-aircraft toggle and model logic; the live sources; the Context-mode exit in the scene director; and the tracking-selection strip in scene policy. The shell was slimmed accordingly: `applicationShell`, `shellFacade`, `visualSettings`, `displayBindings`, `navigationController`, `locationNavigation`, `layerBindings`, `panelChrome` and `panelLayoutController` lost their cockpit/context/detection/tracking wiring, the voice-era facade methods nobody called (`getControlState`, `setBloom`, `applyCinematicPreset` and others) were deleted, and `clearSelectedLayers` moved into the shell. Kept on purpose: `detectionPolicy`/`detectionCohort` (pure allocation policy the world-overlay label arbiter still uses), the world overlay itself, `celestialRing`. The right rail element stays as the Display panel's host; the allocation-benchmark runner mode and its two probes were deleted; the unit runner now has a per-test timeout | 1452 tests, 1449 pass, 0 fail, 3 skipped (from 2649) | renders, 0 errors; screenshot: HUD, ring, dock, Data Layers, Scenes and Display panels intact, Context and Cameras panels gone | layer registry now: bhote-koshi x2, directions, local-dams, local-datacenters. Debt for B15/B16: CSS for removed panels, layer-state tracking machinery (inert, `SHARE_TRACKING_RESTORE_POLICIES` empty), scene document schema still tolerates a `detection` field, HUD `AIS` readout, 'TOP SECRET' banner copy |
| B11a + B12a | Removed geocoding and everything that fed on it: the Google, Photon and Nominatim geocoders, the HTTP geospatial provider, the Overpass feature and road sources, the annotation engine, renderers, resolver and draw tool (with their DISPLAY hooks), the Directions layer and route data, the regional brief, and the whole `services/` request layer (terrain heights, ground floor, mesh floor, ground snap, `/api/*` clients) with `app/operations.js`. Search now answers from coordinates and the bundled city presets only, so it never leaves the machine. `searchAndFlyTo` was reduced to that: no viewport bias, no nearby-place recovery, no building-footprint lookup. The Nepal boundary resolver that came from the annotation resolver is gone, so the Bhote Koshi locator uses its default until B13 removes it | 1142 tests, 1142 pass (3 skipped) | renders, 0 errors | layer registry: bhote-koshi x2, local-dams, local-datacenters. Google 3D Tiles and the map stack remain for B11b |
| B13 + C1 + C3 | The last upstream layers are gone and the first Nexus ones are in. **Removed:** the Bhote Koshi event and locator layers with their data, media (`public/events`), scene pack and CSS; the datacenter and dam layers and the local GeoJSON engine with its bundled datasets; the world-overlay label engine, its arbiter and cohort/policy modules; the aircraft `public/models`. **C1:** TypeScript for new code only (`tsconfig.json`, strict, `erasableSyntaxOnly`, checked by `npm run typecheck`); Node runs `*.test.ts` natively, so the unit runner, formatter and dead-module checker now accept `.ts`. **C3:** `src/nexus/oilStops.ts` is generated from `investigations/02-oil-system/geography.json` by `scripts/generate-oil-stops.mjs` (`npm run check:generated` fails on drift); `oilGeographyLayer.ts` provides two layers, `nexus-oil-stops` (six sourced anchors) and `nexus-oil-corridors` (two sourced lines), each record carrying its caveat and source into the entity description; `oilTour.ts` builds the scene recipe that the director plays, one shot per stop, and is now the only shipped recipe. Registry, catalogue, layer panel and boundary check know the new ids | 854 unit tests (851 pass, 3 skipped) including 9 TypeScript tests; `tsc` clean; generated file matches source | 2 smoke tests pass: the shell renders quietly, and enabling the two oil layers adds 6 and 2 entities and disabling removes them (screenshot over the Gulf shows the Hormuz anchor and label) | layer count 2. Registry tests use the real Nexus ids as stand-ins. The tour is loadable from the Scenes panel; a Playwright test that plays it comes with C7. Map stacks still include Google 3D, Bing and ion until B11b |
| B11b + B14 | Removed Google 3D Tiles, the Bing and Cesium ion stacks and world terrain (`google3d`, `availability`, `mapStartup`, ion imagery and terrain factories), leaving the two keyless stacks: Esri Satellite (falls back to OSM) and OSM, over Re:Earth terrain. Removed the key-setup wizard (`keySetup*`, the Provider Settings template and CSS, the key tooltip on layer rows, the key plumbing through `main.js`, the standalone application and the Vite config's `define`s). Share-link and visual-state map defaults are now `esri-imagery`; the scene shot presentation no longer has photoreal or imagery-comparison rules | 818 tests, 815 pass, 0 fail, 3 skipped | 2 smoke tests pass; hosts unchanged (localhost, Esri, Re:Earth) | the map-stack chip tests were rewritten for two stacks. Nothing in the shell reads a key or an env var now |
| B15 | Trimmed the CSS with a new tool, `scripts/nexus-prune-css.mjs`, that drops every rule whose selectors name an id or class no markup or script mentions (keeping Cesium's and run-time-built prefixes). Stylesheets went from 261 KB to 157 KB: `cctv.css`, `cockpit.css` and `voice-cost.css` are gone, `radio.css` was renamed `data-toggles.css` because what survived is the Data Layers row styling, and the remaining files lost 710 rules. Also removed the DISPLAY panel's Draw section that B12a left behind, and the `global-context-panel` share token | 817 tests, 814 pass, 0 fail, 3 skipped | 2 smoke tests pass; screenshot with the Data Layers and Display panels open looks like the baseline minus the removed controls | The pruner reads text, so a comment that mentions a class keeps its rules alive; a later manual pass can drop those |
| B16a | Finished the tracking-restore removal and cleaned up around it: the share-restore code no longer refers to tracking policies (empty conditionals left by the cut are gone), the facade forwards nothing to a removed handler, two notice tests that asserted that handler's source were dropped, the import-direction checker lost its voice, gevActions and local-GeoJSON rules (and their test), and the unused `readLayerSource` helper was deleted. Camera-verb interruption and its tests went earlier in this step | 787 tests, 784 pass, 0 fail, 3 skipped | 2 smoke tests pass; hosts unchanged | 5 modules remain in the dead-module baseline; all are test-only helpers or compatibility facades that tests import. The one recorded dangling import is a string inside a fixture in the boundary test |
| B16b | Replaced the God's Eye identity and every fabricated readout. **Branding:** the page title, header, loader and subtitle now say Nexus; `public/logo.svg` is a new mark (a ring, and a globe with a small network that the pointer-gaze effect still moves) instead of upstream's eye; package name and description; README rewritten for the shell. **Honest HUD:** the classification banners ("TOP SECRET // SI-TK // NOFORN"), the random mission, sensor, orbit and pass ids, the REC light, GSD and NIIRS, collection time, band/bits/level and the AIS line are gone; what remains is measured from the camera or the clock (UTC time, MGRS, lat/lon, altitude above mean sea level, an estimated sun elevation, tilt from straight down), with a top bar that says "not a live feed". **Start view and places:** the shell opens on the Indian Ocean from 12,000 km instead of flying to Austin; the nine upstream city presets (`CITY_POIS`, now `PLACE_PRESETS`) are replaced by two destinations built from the sourced oil stops (`src/nexus/oilPlaces.ts`). **Credits:** `dataCredits.js` lists the sources the shell really uses (Re:Earth terrain, EGM96, Wikidata, EIA). **Docs:** deleted upstream's CHANGELOG, CONTRIBUTING, DATA_SOURCES, SECURITY, TESTING, all of `docs/` (650 KB, describing removed features), the four CCTV source files in `config/` and the test that asserted those docs. Git history keeps them at the import commit | 796 tests, 793 pass, 0 fail, 3 skipped; tsc clean | 3 smoke tests pass; hosts unchanged; screenshot of the start view checked | internal `gev` event and storage-key prefixes are kept (they are identifiers, not copy). Logo is my own simple mark and can be replaced |
| C4 | Static corner readouts. `src/nexus/readouts.ts` builds three lines from the generated pack summary (`OIL_PACK`, now emitted by `generate-oil-stops.mjs` beside the geography): the as-of date (the pack's `checked_on`), the pack id and schema, and how many pack entities the map places (8 of 23, so 15 not on the map). The HUD paints them top right and adds a selection line bottom right (`SELECTED: <label> · <PRECISION>`, `NONE`, or `NO STATED PRECISION` for anything that is not a Nexus record). They live in the HUD, so the existing H toggle hides them and they are on by default | +6 tests (readouts, HUD wiring, oil places); 793 pass | third smoke test reads the lines and selects the Hormuz anchor | the count is of pack entities that no stop or corridor names, so it rises when the pack grows without the fixture. The pack version is the schema version; the pack has no separate release number yet |
| C2 + C7 | The action layer and global-context mode. `src/nexus/actions.ts` is a verb registry that imports nothing: `fly_to_place`, `set_layer`, `play_tour`, `stop_tour`, `global_context`, `reset_view`. Each verb checks its arguments (declared keys only, own properties only, ids must exist in the catalogue, integers in range), unknown names are refused (including `constructor` and `__proto__`), and `run` never throws. A verb reaches the shell only through `ShellControls` (camera, layers, tour), so it has no way to make a request or write evidence; a test scans the source for that. `shellControls.ts` holds the logic, including global context: entering snapshots the camera pose and the oil layers' visibility, pulls out to the globe and turns the layers on; exiting restores both exactly (layers that were off go back off; entering twice keeps the first snapshot). `shellPorts.ts` connects the ports to the viewer, the layer manager (origin `tool`) and the scene director; `shellActions.ts` assembles it and `app/tools.js` exposes it as `window.__godsEyeView.actions` | +12 tests (7 registry, 5 controls); 805 pass; tsc clean | fourth smoke test: refused verbs, fly to Hormuz, global context out and back to within 0.5 degrees with layers restored, and the tour plays to the first stop with both layers on | the director's `startScene` settles when the whole run ends, so `playTour` reads `running` instead of awaiting it. Nothing in the shell calls the registry yet except the test: the panels still call their own functions, to be moved over when a second caller (chat) exists |
| C5 | Share links, checked and hardened. The upstream link already carries camera, style, layers, panels and map in the hash. New tests (`src/sharelink.test.mjs`) pin a round trip (a camera and layer state written to a hash reads back the same) and a set of hostile hashes. They found real gaps, now fixed in `parseInitialHash`: latitude and longitude outside the globe were accepted, camera height, heading, pitch and roll were unbounded (`alt=1e300`), and `style=constructor` returned the `Object` function because the lookup read inherited properties. Coordinates are now range-checked, camera values clamped, the style looked up by own property, the HUD variant limited to its three names and the map id to a plain slug | +7 tests; 812 pass | two more smoke tests: a hostile hash loads with no errors and the shell opens on its own view; a well-formed hash restores its camera | layer ids from a hash were already validated against the registry and stay so. Nothing in a hash is ever evaluated: values are parsed as numbers or matched against fixed sets |
| C6 | 2D fallback. `src/nexus/fallbackProjection.ts` (pure) projects Natural Earth land (`world-atlas` land-110m, public domain) with d3-geo and places the six stops and two corridors; `fallbackMap.ts` renders it as SVG with keyboard-selectable markers, a card showing each record's role, precision, caveat and source (all set as text, never markup), and the same readout lines and selection precision as the globe. `main.js` shows it instead of the shell when `?view=2d` is given, when no WebGL context can be created, or when startup fails (with the reason shown). New dependencies: `d3-geo`, `topojson-client`, `world-atlas` (and type packages as dev dependencies); their licences still have to be checked against the cleared list in phase D | +4 tests (projection); 816 pass; tsc clean | two smoke tests: `?view=2d` shows 6 places and 2 corridors, opens a card with the caveat by mouse and by keyboard and starts no globe; with `getContext('webgl')` forced to null the shell falls back by itself | screenshot checked. The fallback replaces the whole page rather than sharing the shell's chrome, so it has none of the panels or the tour |

## How a step is checked

`npm run check:step` runs, in order: the upstream unit tests, the dead-module checker (`--strict`, so it fails on any new dangling import or unreachable module, and on a stale allowed-list entry), the one-engine check, and the Playwright smoke test (renders, and no console error outside `tests/known-errors.json`, which can only shrink). The sandbox GPU is not the user's: a green smoke test means "renders and is quiet", not "looks and performs right". The 40 unreachable modules on the baseline are loaded through computed dynamic imports that a static scan cannot follow; each strip step deletes the ones it owns and removes them from the list.
