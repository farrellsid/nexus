# Upstream and strip log

This folder is a stripped fork of **God's Eye View** by Bilawal Sidhu, <https://github.com/bilawalsidhu/gods-eye-view>, imported at commit **`f01b6a5`** ("fix(ui): stop panel glows from adding scrollbars to the narrow-screen rails (#699)"). Its MIT licence is in `LICENSE`. The MIT grant covers the source code only; upstream states that its third-party data and assets are excluded. See `THIRD_PARTY_NOTICES.md` at the repository root.

Plan: `docs/superpowers/plans/2026-09-24-m5-visual-shell.md`. Branch: `m5-shell`. Each step below is one commit.

## What was left out of the import (deviations from "unmodified")

- `.git`, `node_modules`: not source. (`build/` is source here, not output: `build/application-html.js` is imported by tests, and my first import wrongly excluded it. Restored in A1b.)
- `docs/media/` (about 69 MB of demonstration GIFs) and `.github/` are not imported. Upstream's `docs/*.md` (512 KB) are imported, because 23 test files assert documentation consistency.
- `src/data/local_data/`, `src/data/fixtures/`, `public/models/`, `public/events/` are **on disk but ignored by git** (`.gitignore` here). They are upstream's bundled datasets, test fixtures, 3D models and event media, whose licences are not cleared for this repository. (`src/data/` itself also holds 236 code files. A1 ignored the whole directory by mistake, so those files were absent from history until the correction recorded as row A1c.) The app still needs them to start until the strip steps that own them remove the code that reads them; each such step deletes its files. They never enter git history.

## Step log

| Step | What changed | Tests | Smoke | Notes |
|---|---|---|---|---|
| A1 | Imported the reference code unmodified (apart from the exclusions above) | not yet run | not yet run | 961 tracked files |
| A1b | Restored `build/` and imported `docs/*.md` after a first run showed 23 failing test files caused by my exclusions | 4168 tests, 4158 pass, 0 fail, 10 skipped: identical to the untouched clone | not yet run | the mistake and its fix are recorded rather than hidden |
| A2 | `npm ci` (PUPPETEER_SKIP_DOWNLOAD=1) and first run | as A1b | renders | 15 s install; upstream's lockfile already resolves **one** `@cesium/engine` (22.3.0, `@cesium/widgets` 14.3.0 deduped) |
| A3 | Baseline measurements | as A1b | | `vite build`: 423 files, 31.2 MB in `dist/` (Cesium.js 5.6 MB, egm96 2.7 MB, datacenters.geojsonl 2.5 MB, regions 1.9 MB); `check:boundaries` passes |
| A4 | Added the step checks: Playwright smoke (`tests/smoke.spec.mjs`, `known-errors.json`), `scripts/nexus-dead-modules.mjs` (baseline: 1147 modules, 328 tests, 681 reachable; 2 dangling and 40 unreachable recorded as the allowed list), `scripts/nexus-check-engine.mjs`, and `npm run check:step` | as A1b | 0 console errors; screenshot 755 KB (`.local/m5/a4-baseline.png`) | external hosts at load: `fonts.googleapis.com`, `fonts.gstatic.com`, `services.arcgisonline.com`, `terrain.reearth.land` |
| A5 | Removed the Google Fonts and Material Symbols CDN links. Fonts and a 32-glyph icon subset are self-hosted (`public/fonts`, 189 KB, hashes in `manifest.json`); `src/ui/styles/fonts.css` holds the `@font-face` rules and `icon-glyphs.css` (imported last, as Google's stylesheet was) holds the icon class. `scripts/nexus-fetch-fonts.mjs` reproduces it. The smoke test now asserts the page contacts only `tests/allowed-hosts.json` | 4168 tests, 0 fail | renders, 0 errors; hosts now `127.0.0.1`, `services.arcgisonline.com`, `terrain.reearth.land` | the first attempt put the icon class before the app CSS, which shrank the arrows; found by comparing screenshots, fixed by keeping Google's original cascade order |
| A1c | Found while removing voice code that `.gitignore` excluded all of `src/data/`, which holds 236 source and test files beside the datasets. Narrowed the ignore to `src/data/local_data/` and `src/data/fixtures/` and tracked the code | not re-run | | recorded out of order: this commit lands after B1, so B1's edits to `src/data` tests are inside it and `src/data` has no faithful A1 snapshot in history; the untouched reference is `f01b6a5` |
| B1 | Removed the upstream Node server (`server/`), the Pinokio launcher, dev/QA shell and puppeteer scripts, and the tests whose subject they were; `vite.config.js` is now the browser-only `createBrowserViteConfig` on 127.0.0.1:4173. Pruned 15 dangling `exports` (`scripts/nexus-prune-exports.mjs` reports and fixes them; `check:exports` is now part of `check:step`), 15 boundary groups, the `puppeteer` devDependency and the `track-regression` rig. `ws` and `sharp` stay until the AIS and transit-icon code they serve is removed | 3600 tests, 3593 pass, 0 fail, 7 skipped (568 fewer than A1b: tests deleted with the code they covered) | renders; 2 transitional 404s from `/api/realtime/debug-log` and `/api/openai/hud-summary` recorded in `known-errors.json`, to disappear with the voice code | dead-module baseline re-recorded (58 unreachable, 1 dangling); the smoke test now waits for a rendered frame instead of a fixed delay after one run screenshotted before imagery loaded |
| B2 | Removed voice: `src/voice/` (29 files, including the 2,400-line `gevActions.js`), the microphone dock control's owner, the HUD's OpenAI summary request (the HUD keeps its deterministic summary line), the `summary` request service and its endpoint, `hudSummaryResponse.js`, and `data/analystEngine.js` (only the voice model called it). Tests that read the deleted voice sources were removed or trimmed (three whole tests). New `scripts/nexus-prune-config.mjs` prunes deleted modules from `package.json` exports, `package-boundaries.json` and `format-scope.json` (`check:config` replaces `check:exports`); `format:check` joins `check:step` | 3342 tests, 3335 pass, 0 fail, 7 skipped | renders, 0 errors; both transitional 404s are gone and `known-errors.json` is empty again | screenshot compared with `.local/m5/a4-baseline.png`: same chrome; the mic pill is gone and the dock is shorter. Left for later steps: voice CSS (B15), the first-run tip "GEV MIC button" (B14), `check-import-directions.mjs` voice rules (B16) |
| B3.1 | Removed the submarine-cable layer (TeleGeography data, licence CC BY-NC-SA, uncleared): `layers/submarineCables`, its app constructor and bundled source, the layer-state token, catalogue entry, panel entry, credit line and the on-disk dataset. Also deleted the world-overlay allocation probe (`worldOverlayAllocation.{test,worker}.mjs`, 1,700 lines) because it benchmarks every live layer and would need edits at each step; the overlay itself stays | 3314 tests, 3307 pass, 0 fail, 7 skipped | renders, 0 errors | layer count assertions 21 to 20; `ALLOCATION_TEST_FILES` now holds one file |
| B3.2 (from B14) | Removed the first-run mission launcher early (`firstRunExperience.js`, its test, `welcome.html`, `first-run.css`). Its tiles (contacts, space missions, environmental) each turned on layers the next steps delete, so keeping it would have forced an edit at every step. `startApplicationChrome` keeps the reveal sequence and now takes the welcome initializer only by injection, which is where Nexus's own first-run will plug in (C) | 3282 tests, 3275 pass, 0 fail, 7 skipped | renders, 0 errors; the modal is gone (screenshot 1.28 MB) | B14 now only has the key-setup wizard left |
| B3.3 | Removed the earthquakes layer (USGS): `layers/earthquakes`, its app constructor and compat module, the reference-source factory that only served it and cables, the layer-state token, panel entry, credit and recipe keys. Registry tests that used `earthquakes` as a stand-in id now use `local-dams` (token `q`), which survives until B13 | 3267 tests, 3260 pass, 0 fail, 7 skipped | renders, 0 errors | layer count 19; `app/layers/overlayHost.js` deleted (only the quake constructor used it) |
| B3.4 | Removed the ground-mobility layers: bikeshare (GBFS), street traffic (TomTom flow, Overpass roads) and transit (GTFS-Realtime), with their compat data modules, camera-sensitivity claims, history store, feed credits and layer-state tokens. Removed the traffic sync chip (`shellFeedback`, `loadingFeedback` reducer, markup element, element lookups) and the traffic hooks in world-jump navigation; the loading ticker survives as `_startFeedbackTicker` and the timer is now `_worldJumpTimer`. New tool for test edits: a parser-based test remover (scratch), because a brace matcher had mangled a file earlier | 2945 tests, 2938 pass, 0 fail, 7 skipped (322 fewer: the transit lifecycle suite alone was ~3,700 lines) | renders, 0 errors | layer count 16. Detection still carries transit bracket painting and `worldOverlayTokens` transit tier colours until B12; CSS for the chip until B15; layer-registry tests now use `local-datacenters` as the stand-in id |

## How a step is checked

`npm run check:step` runs, in order: the upstream unit tests, the dead-module checker (`--strict`, so it fails on any new dangling import or unreachable module, and on a stale allowed-list entry), the one-engine check, and the Playwright smoke test (renders, and no console error outside `tests/known-errors.json`, which can only shrink). The sandbox GPU is not the user's: a green smoke test means "renders and is quiet", not "looks and performs right". The 40 unreachable modules on the baseline are loaded through computed dynamic imports that a static scan cannot follow; each strip step deletes the ones it owns and removes them from the list.
